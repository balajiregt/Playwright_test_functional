pipeline {
    agent any

    stages {
        stage('e2e-tests') {
            steps {
                // Execute the 'ci' script from the root of your repository.
                sh './ci'
                
                // Run Playwright tests.
                sh 'npx playwright test'
            }
        }
    }
}
